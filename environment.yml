# conda env create --file environment.yml
name: eda_datos
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.12
  - pip:
    - pyhere
    